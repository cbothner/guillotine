<% if @reward.errors.any? %>
  alert("<%= raw escape_javascript @reward.errors.full_messages.to_sentence %>");
<% else %>
  $("aside.pledgerRewards.grid_3").replaceWith('<%= j render_cell :reward_list, :show, pledger: @reward.pledger %>');
  window.editedtext = false
<% end %>
