<section class="grid_2">
  &nbsp;
</section>
<section class="home grid_8">
  <% if content_for? :back %>
    <%= link_to "&larr;Back".html_safe, content_for(:back) %>
  <% else %>
    <%= link_to "&larr;Back".html_safe, :back %>
  <% end %>
  <%= yield %>
</section>

<%= parent_layout 'application' %>
