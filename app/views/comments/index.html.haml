-title "Comments"
-Time.zone="EST"
-@comments.each do |comment|
  .commentLine.flexcontainer
    .sidemargins.comment_time.flex1="#{Time.zone.at(comment.created_at).strftime("%l:%M %p")}<br />#{Time.zone.at(comment.created_at).strftime('%e %b %Y')}".html_safe
    .sidemargins.flex5
      .commentText="\"#{comment.comment}\""
      .commentShowName{:style => "margin-left:10px"}
        =link_to(comment.pledger.name,comment.pledger)
        =' &rarr;'.html_safe
        =link_to(comment.show.get_name,comment.show)
